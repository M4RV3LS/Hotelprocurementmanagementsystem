# 🏛️ System Architecture

## Overview

This document provides a visual and textual overview of the Hotel Procurement Management System architecture after the database integration fix.

## 🔄 Data Flow Architecture

### Application Layer Structure

```
┌─────────────────────────────────────────────────────────────┐
│                        React App (App.tsx)                    │
│                                                               │
│  ┌──────────┐  ┌──────────┐  ┌──────┐  ┌──────┐  ┌──────┐ │
│  │Dashboard │  │  Config  │  │ListPR│  │ListPO│  │Approval││
│  │          │  │          │  │      │  │      │  │        ││
│  └────┬─────┘  └────┬─────┘  └──┬───┘  └──┬───┘  └───┬────┘│
│       │             │            │         │          │      │
│       └─────────────┴────────────┴─────────┴──────────┘      │
│                             │                                 │
│                      useConfigData Hook                       │
│                             │                                 │
└─────────────────────────────┼─────────────────────────────────┘
                              │
┌─────────────────────────────┼─────────────────────────────────┐
│                    API Layer (utils/api.ts)                   │
│                             │                                 │
│  ┌──────────────┐  ┌───────┴────────┐  ┌─────────────────┐ │
│  │ PRs API      │  │  Items API     │  │   Vendors API   │ │
│  │              │  │  ✅ FIXED      │  │                 │ │
│  ├──────────────┤  ├────────────────┤  ├─────────────────┤ │
│  │ • getAll()   │  │ • getAll()     │  │  • getAll()     │ │
│  │ • save()     │  │ • save()       │  │  • save()       │ │
│  │ • delete()   │  │ • delete()     │  │  • delete()     │ │
│  └──────┬───────┘  └────────┬───────┘  └────────┬────────┘ │
│         │                   │                    │           │
│         └───────────────────┴────────────────────┘           │
│                             │                                 │
│                   Supabase Client                            │
│                  (Direct Connection)                         │
└─────────────────────────────┼─────────────────────────────────┘
                              │
┌─────────────────────────────┼─────────────────────────────────┐
│                       Supabase Backend                        │
│                             │                                 │
│  ┌──────────────────────────┴──────────────────────────────┐ │
│  │                    PostgreSQL Database                   │ │
│  │                                                           │ │
│  │  ┌────────────┐  ┌────────────┐  ┌─────────────────┐  │ │
│  │  │   Items    │  │  Vendors   │  │  Procurement    │  │ │
│  │  │   Table    │  │   Table    │  │  Requests Table │  │ │
│  │  └────────────┘  └────────────┘  └─────────────────┘  │ │
│  │                                                           │ │
│  │  ┌────────────┐  ┌────────────┐  ┌─────────────────┐  │ │
│  │  │   POs      │  │ Activity   │  │   Request       │  │ │
│  │  │   Table    │  │ Logs Table │  │   Items Table   │  │ │
│  │  └────────────┘  └────────────┘  └─────────────────┘  │ │
│  └───────────────────────────────────────────────────────┘ │
│                                                               │
│  ┌───────────────────────────────────────────────────────┐  │
│  │                  Supabase Storage                      │  │
│  │                                                         │  │
│  │  📁 Delivery Proof Bucket                             │  │
│  │     • Delivery proof documents                         │  │
│  │     • Rejection proof files                            │  │
│  │     • Signed PO documents                              │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

## 🔧 Before vs After: The Fix

### ❌ Before (Broken Architecture)

```
┌──────────────┐
│  Component   │
└──────┬───────┘
       │
       ▼
┌──────────────┐
│ itemsAPI     │
└──────┬───────┘
       │
       ▼ fetch("/make-server-1e4a32a5/items")
┌──────────────┐
│   Server     │ ◄── ❌ Endpoint doesn't exist
│   Endpoint   │ ──► Returns HTML error page
└──────┬───────┘
       │
       ▼ Response: "<!DOCTYPE html>..."
┌──────────────┐
│ JSON.parse() │ ◄── ❌ Can't parse HTML as JSON
└──────┬───────┘
       │
       ▼
   ❌ ERROR!
"Unexpected token '<'"
```

### ✅ After (Fixed Architecture)

```
┌──────────────┐
│  Component   │
└──────┬───────┘
       │
       ▼
┌──────────────┐
│ itemsAPI     │
└──────┬───────┘
       │
       ▼ supabase.from("master_items").select("*")
┌──────────────┐
│   Supabase   │
│   Client     │ ◄── ✅ Direct database connection
└──────┬───────┘
       │
       ▼ PostgreSQL Query
┌──────────────┐
│   Database   │ ──► Returns data rows
└──────┬───────┘
       │
       ▼
   ✅ SUCCESS!
   Array of items
```

## 📊 Database Schema

### Entity Relationship Diagram

```
┌─────────────────────┐
│  payment_methods    │
│  ─────────────────  │
│  • id (PK)          │
│  • name (UNIQUE)    │
│  • is_active        │
└─────────────────────┘

┌─────────────────────┐
│  item_categories    │
│  ─────────────────  │
│  • id (PK)          │
│  • name (UNIQUE)    │
└──────┬──────────────┘
       │ 1
       │
       │ many
       ▼
┌─────────────────────┐         ┌─────────────────────────────┐
│  master_items       │◄────────┤  vendor_catalog_items      │
│  ─────────────────  │  many   │  ────────────────────────  │
│  • id (PK)          │         │  • id (PK)                  │
│  • code (UNIQUE)    │         │  • vendor_id (FK)           │
│  • name             │         │  • item_id (FK)             │
│  • category_id (FK) │         │  • unit_price               │
│  • brand_name       │         │  • price_type               │
│  • uom              │         │  • min_quantity             │
│  • photos (JSONB)   │         │  • wht_percentage           │
└──────┬──────────────┘         └────────┬────────────────────┘
       │ 1                                │ many
       │                                  │
       │ many                             │ 1
       ▼                                  ▼
┌─────────────────────┐         ┌─────────────────────────────┐
│  request_items      │         │  vendors                    │
│  ─────────────────  │  many   │  ────────────────────────  │
│  • id (PK)          ├────────►│  • id (PK)                  │
│  • request_id (FK)  │    1    │  • code (UNIQUE)            │
│  • master_item_id(FK)│        │  • name                     │
│  • assigned_vendor_id (FK)    │  • region                   │
│  • purchase_order_id (FK)     │  • email                    │
│  • status           │         │  • phone                    │
│  • quantity         │         │  • payment_methods (JSONB)  │
│  • unit_price       │         │  • agreements (JSONB)       │
│  • total_price      │         └─────────────────────────────┘
│  • po_number        │                  ▲
└──────┬──────────────┘                  │ 1
       │ many                             │
       │                                  │ many
       │ 1                                │
       ▼                                  │
┌─────────────────────┐                  │
│ procurement_requests│                  │
│  ─────────────────  │                  │
│  • id (PK)          │                  │
│  • pr_number (UNIQUE)                  │
│  • pr_date          │                  │
│  • property_name    │                  │
│  • requestor_email  │                  │
│  • status           │                  │
│  • region           │                  │
└──────┬──────────────┘                  │
       │ 1                                │
       │                                  │
       │ many                             │
       ▼                                  │
┌─────────────────────┐                  │
│  activity_logs      │                  │
│  ─────────────────  │                  │
│  • id (PK)          │                  │
│  • request_id (FK)  │                  │
│  • user_email       │                  │
│  • action           │                  │
│  • details          │                  │
│  • timestamp        │                  │
└─────────────────────┘                  │
                                         │
┌─────────────────────┐                  │
│  purchase_orders    ├──────────────────┘
│  ─────────────────  │  1
│  • id (PK)          │
│  • po_number (UNIQUE)
│  • vendor_id (FK)   │
│  • generated_date   │
│  • total_amount     │
│  • status           │
│  • approval_status  │
│  • delivery_proofs (JSONB)
└─────────────────────┘
```

## 🔐 Security Architecture

### Row Level Security (RLS) Flow

```
┌─────────────────────────────────────────────────────────┐
│                    Client Request                        │
│              (with Supabase Anon Key)                    │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│                   Supabase Edge                          │
│              • Validates API Key                         │
│              • Checks Authentication                     │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│                Row Level Security                        │
│              • Evaluates RLS Policies                    │
│              • Checks User Permissions                   │
│              • Applies Filters                           │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│                PostgreSQL Database                       │
│              • Executes Query                            │
│              • Returns Authorized Data                   │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│                    Response                              │
│              (Filtered by RLS Policies)                  │
└─────────────────────────────────────────────────────────┘
```

### Current RLS Policy (Development)

```sql
-- Permissive policy for development
CREATE POLICY "Allow all operations" 
ON table_name
FOR ALL 
USING (true) 
WITH CHECK (true);
```

### Production RLS Example

```sql
-- Users can only see their own PRs
CREATE POLICY "users_own_prs" 
ON procurement_requests
FOR SELECT 
USING (requestor_email = auth.jwt()->>'email');

-- Users can only update their own PRs if status is 'Draft'
CREATE POLICY "users_update_own_drafts" 
ON procurement_requests
FOR UPDATE 
USING (
  requestor_email = auth.jwt()->>'email' 
  AND status = 'Draft'
);

-- Procurement team can see all PRs
CREATE POLICY "procurement_team_all" 
ON procurement_requests
FOR ALL 
USING (
  auth.jwt()->>'role' = 'procurement'
);
```

## 📦 Component Architecture

### Component Hierarchy

```
App.tsx (Root)
│
├── ProcurementDashboard
│   ├── RequestDetailModal
│   │   ├── ItemDetailSection
│   │   └── LogActivity
│   ├── GeneratePOModal
│   └── StatusBadge
│
├── Configuration
│   ├── VendorManagement
│   │   └── VendorFormModal
│   ├── ItemConfiguration
│   │   └── ItemFormModal
│   ├── ItemCategoryConfiguration
│   └── PaymentMethodConfiguration
│
├── ListPR
│   ├── RequestDetailModal
│   ├── VendorAssignmentForm
│   └── GeneratePOModal
│
├── ListPO
│   ├── POPreviewModal
│   ├── UploadSignedPOModal
│   ├── LinkProofModal
│   ├── MarkDeliveredModal
│   └── RejectRequestModal
│
└── RequestApproval
    └── RequestApproval Component
```

## 🔄 State Management Flow

### useConfigData Hook Flow

```
┌─────────────────────────────────────────────────────────┐
│                   Component Mounts                       │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│              useConfigData Hook Initializes              │
│              • Sets isLoading = true                     │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│                  loadData() Called                       │
│              Promise.all([                               │
│                vendorsAPI.getAll(),                      │
│                itemsAPI.getAll(),                        │
│                paymentMethodsAPI.getAll()                │
│              ])                                          │
└────────────────────┬────────────────────────────────────┘
                     │
        ┌────────────┴────────────┐
        ▼                         ▼
┌──────────────┐          ┌──────────────┐
│  Success     │          │    Error     │
└──────┬───────┘          └──────┬───────┘
       │                         │
       ▼                         ▼
┌──────────────┐          ┌──────────────┐
│ Set State:   │          │ Set State:   │
│ • vendors    │          │ • error      │
│ • items      │          │ • empty []   │
│ • payments   │          └──────────────┘
│ • isLoading=false
└──────────────┘
```

## 🗄️ Storage Architecture

### Supabase Storage Structure

```
Supabase Storage
│
└── 📁 Delivery Proof (Bucket)
    │
    ├── 📁 {po_id}/
    │   ├── 📄 {timestamp}_proof_document.pdf
    │   ├── 📄 {timestamp}_delivery_photo.jpg
    │   └── 📄 {timestamp}_invoice_scan.pdf
    │
    ├── 📁 Rejections/
    │   └── 📁 {item_id}/
    │       ├── 📄 {timestamp}_rejection_photo.jpg
    │       └── 📄 {timestamp}_rejection_document.pdf
    │
    └── 📁 SignedPOs/
        └── 📄 {po_number}_signed.pdf
```

### File Upload Flow

```
┌─────────────────────────────────────────────────────────┐
│               User Selects File                          │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│            purchaseOrdersAPI.uploadFile()                │
│            • Sanitizes filename                          │
│            • Generates unique path                       │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│          supabase.storage.from(bucket)                   │
│                  .upload(path, file)                     │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│            File Stored in Supabase                       │
│            Public URL Generated                          │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│         URL Saved to Database                            │
│         (delivery_proof_id, signed_po_link, etc.)       │
└─────────────────────────────────────────────────────────┘
```

## 🎯 Request Lifecycle

### Complete PR → PO → Delivery Flow

```
1. CREATE PR
   ┌──────────────┐
   │ User fills   │
   │ PR form      │
   └──────┬───────┘
          │
          ▼
   procurement_requests table
   request_items table
   Status: "Waiting Vendor Assignment"

2. ASSIGN VENDORS
   ┌──────────────┐
   │ Procurement  │
   │ assigns      │
   │ vendors      │
   └──────┬───────┘
          │
          ▼
   request_items.assigned_vendor_id updated
   Status: "Waiting PO"

3. GENERATE PO
   ┌──────────────┐
   │ Generate PO  │
   │ for vendor   │
   └──────┬───────┘
          │
          ▼
   purchase_orders table created
   request_items.purchase_order_id updated
   Status: "Waiting PO Approval"

4. APPROVE PO
   ┌──────────────┐
   │ Upload       │
   │ signed PO    │
   └──────┬───────┘
          │
          ▼
   purchase_orders.signed_po_link updated
   purchase_orders.approval_status = "Approved"
   Status: "Process by Vendor"

5. DELIVERY
   ┌──────────────┐
   │ Upload proof │
   │ Mark delivered│
   └──────┬───────┘
          │
          ▼
   purchase_orders.delivery_proofs updated
   request_items.delivery_proof_id updated
   Status: "Delivered"

6. CLOSURE
   ┌──────────────┐
   │ All items    │
   │ delivered    │
   └──────┬───────┘
          │
          ▼
   purchase_orders.status = "Close"
   procurement_requests.status = "Close"
```

## 📈 Performance Considerations

### Database Indexes

```
master_items
  ├── idx_master_items_code (code)
  └── idx_master_items_category_id (category_id)

vendors
  └── idx_vendors_code (code)

vendor_catalog_items
  ├── idx_vendor_catalog_vendor_id (vendor_id)
  └── idx_vendor_catalog_item_id (item_id)

procurement_requests
  ├── idx_procurement_requests_pr_number (pr_number)
  └── idx_procurement_requests_status (status)

request_items
  ├── idx_request_items_request_id (request_id)
  ├── idx_request_items_master_item_id (master_item_id)
  ├── idx_request_items_vendor_id (assigned_vendor_id)
  ├── idx_request_items_po_id (purchase_order_id)
  └── idx_request_items_status (status)

activity_logs
  ├── idx_activity_logs_request_id (request_id)
  └── idx_activity_logs_timestamp (timestamp DESC)

purchase_orders
  ├── idx_purchase_orders_po_number (po_number)
  ├── idx_purchase_orders_vendor_id (vendor_id)
  └── idx_purchase_orders_status (status)
```

### Query Optimization

```sql
-- Good: Uses index
SELECT * FROM procurement_requests 
WHERE pr_number = 'PR-001';

-- Good: Uses index
SELECT * FROM request_items 
WHERE status = 'Waiting Vendor Assignment';

-- Optimized: Reduces data transfer
SELECT pr_number, status, pr_date 
FROM procurement_requests
ORDER BY created_at DESC
LIMIT 50;
```

## 🔍 Monitoring & Debugging

### Key Metrics to Monitor

1. **Query Performance**
   - Average query time
   - Slow queries (>100ms)
   - Connection pool usage

2. **Storage Usage**
   - Total files uploaded
   - Storage size
   - Upload success rate

3. **Error Rates**
   - API errors by type
   - Database errors
   - Frontend errors

4. **User Actions**
   - PRs created per day
   - POs generated per day
   - Average PR → PO time

### Debug Flow

```
1. Check Browser Console
   └─► Frontend errors, network issues

2. Check Supabase Logs
   └─► Database errors, query performance

3. Check Network Tab
   └─► API call timing, payload sizes

4. Check Database
   └─► Data integrity, constraint violations
```

## 🎊 Conclusion

This architecture provides:
- ✅ Direct database access (no HTTP errors)
- ✅ Proper separation of concerns
- ✅ Scalable data model
- ✅ Security through RLS
- ✅ Efficient queries with indexes
- ✅ Clear component hierarchy
- ✅ Robust error handling

The system is production-ready with proper authentication and RLS configuration!
